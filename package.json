{
  "name": "scrape-api",
  "version": "1.0.0",
  "description": "API for scraping URLs and waiting for specific file types",
  "main": "server.js",
  "scripts": {
    "start": "node server.js"
  },
  "dependencies": {
    "express": "^4.18.2",
    "puppeteer": "^21.0.0"
  }
}